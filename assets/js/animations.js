/**
 * SKY EDU - Animations & Micro-interactions (LEGACY STUB)
 *
 * File gốc (576 dòng) đã được xác minh là dead code — không file HTML nào
 * `<script src="assets/js/animations.js">` và không có nơi nào gọi
 * `Animations.init()` hoặc các method của nó trong repo.
 *
 * Tuy nhiên, do KHÔNG CÓ GIT HISTORY, không thể đọc lại nội dung cũ để
 * xác minh 100% an toàn. File stub này được khôi phục như một placeholder
 * để tránh 404 nếu một số trang / script / extension nào đó vô tình
 * tham chiếu đến `Animations` global.
 *
 * Nếu bạn thực sự cần chức năng animation trước đây, vui lòng:
 *   1. Khôi phục từ Git history nếu có.
 *   2. Hoặc đã có sẵn trong code inline:
 *      - Magnetic button: index.html (line ~750)
 *      - Scroll animations: CSS trong style.css / premium-style.css
 *      - Counter animations: dashboard.html (DashPage._animateCounter)
 */

window.Animations = {
    init: function () { /* no-op: xem comment ở đầu file */ },
    setupScrollAnimations: function () {},
    setupButtonEffects: function () {},
    setupCardAnimations: function () {},
    setupNumberCounters: function () {},
    setupTypingEffect: function () {},
    setupParallax: function () {},
    setupHoverEffects: function () {},
    injectAnimationsCSS: function () {}
};